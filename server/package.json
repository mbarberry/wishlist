{
  "version": "2.0",
  "private": true,
  "author": "Cain Barberry",
  "main": "lambda.mjs",
  "engines": {
    "node": "^18"
  },
  "scripts": {
    "fullbuild": "npm run docker:login && npm run docker:build && npm run docker:push && npm run sam:build && npm run sam:deploy",
    "docker:login": "aws ecr get-login-password --region us-west-2 | docker login --username AWS --password-stdin $ECR_ID.dkr.ecr.us-west-2.amazonaws.com",
    "docker:build": "docker build -t $ECR_ID.dkr.ecr.us-west-2.amazonaws.com/wishlists:$BUILD_ID .",
    "docker:push": "docker push $ECR_ID.dkr.ecr.us-west-2.amazonaws.com/wishlists:$BUILD_ID",
    "sam:build": "sam build --config-env=$CONFIG_ENV",
    "sam:deploy": "sam deploy --config-env=$CONFIG_ENV"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    "mongodb": "^5.4.0"
  },
  "devDependencies": {
    "eslint-config-react": "^1.1.7",
    "set-env": "file:utils/set-env-1.3.tgz"
  }
}
